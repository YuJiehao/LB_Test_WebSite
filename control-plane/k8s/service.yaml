apiVersion: v1
kind: Service
metadata:
  name: control-plane-service
  labels:
    app: control-plane
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: control-plane
